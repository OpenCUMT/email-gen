---
export interface Props extends HTMLAttributes<"a"> {}

const styles = {
    color: "#0991ed",
    "text-decoration": "none",
    cursor: "pointer"
};

const { style, ...rest }: Props = Astro.props;
---

<a style={Object.assign({}, styles, style)} {...rest} target="_blank"><slot /></a>
